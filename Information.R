# Package List
packs <- available.packages()

# Package Installation
# Multiple packages can be installed using a character array - c("pack1", "pack2")
install.packages("PackageName")

# Load Package
library(package)    # No quote around package

# Display current open packages
search()